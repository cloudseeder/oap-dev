{
  "oap": "1.0",
  "name": "oap-email",
  "description": "Search and read cached email. Use for 'check my email', 'any new messages?', 'emails from Jane', 'email summary since yesterday', 'unread messages'. Messages are categorized: use category='personal' for real human emails (from people — use for 'real emails', 'my emails', 'important emails'), 'machine' for automated/system alerts, 'mailing-list' for newsletters/digests, 'spam' for junk, 'offers' for sales/deals. When the user asks for 'real emails' or 'my emails', ALWAYS set category='personal'. Messages are cached locally from IMAP — use 'list' to search/filter or 'summary' for an overview. Do NOT call 'scan' unless the user explicitly asks to refresh from the mail server.",
  "input": {
    "format": "application/json",
    "description": "JSON with 'action' to select operation. Prefer 'list' or 'summary' — they query the local cache (already populated by background scans). Actions: 'list' (search/filter cached messages), 'summary' (activity overview with counts and senders), 'get' (single message by id), 'thread' (all messages in thread), 'scan' (fetch new from IMAP server — rarely needed).",
    "parameters": {
      "action": {"type": "string", "description": "Operation: 'list', 'get', 'thread', 'summary', 'classify', 'scan'", "required": true},
      "id": {"type": "string", "description": "Message ID (for get action)", "required": false},
      "thread_id": {"type": "string", "description": "Thread ID (for thread action)", "required": false},
      "since": {"type": "string", "description": "ISO 8601 timestamp — only messages after this time", "required": false},
      "limit": {"type": "integer", "description": "Max results (1-100, default 20)", "required": false},
      "unread": {"type": "boolean", "description": "Only unread messages (default false)", "required": false},
      "folder": {"type": "string", "description": "IMAP folder (default INBOX)", "required": false},
      "query": {"type": "string", "description": "Search with OR support and field prefixes. Examples: 'Amy Brooks OR Keric Brooks', 'from:amy@netgate.net', 'from:Amy subject:car', 'subject:invoice'. Prefixes: from/sender, to, subject, body. Without prefix searches all fields.", "required": false},
      "category": {"type": "string", "description": "Filter by category: 'personal' (real human emails), 'machine' (server alerts/automated), 'mailing-list' (newsletters/digests), 'spam' (junk), 'offers' (sales/deals). Omit for all.", "required": false}
    }
  },
  "output": {
    "format": "application/json",
    "description": "Message objects with sanitized plain text bodies, sender/recipient info, thread IDs, and attachment metadata (no content). Summary returns counts, unique senders, and recent subjects."
  },
  "invoke": {
    "method": "POST",
    "url": "http://localhost:8305/api"
  },
  "tags": ["email", "inbox", "imap", "communication"]
}
